(function() { // <-- IIFE

var app = angular.module("wordApp");


app.factory("wordApp", function() {
    // When using a factory, return an object.
    return {
        toForm: function(wordForm) {
            return (toForm)
        toDisplay: function(wordDisplay) {
            return (toDisplay)
        }
    };
});






})();
